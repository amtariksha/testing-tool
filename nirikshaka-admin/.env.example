# Same Supabase Postgres as the main dashboard
DATABASE_URL=postgresql://user:password@host:5432/postgres

# REQUIRED — admin panel access PIN. Long and random; there is no default.
# Rotating it invalidates all admin sessions.
ADMIN_PIN=
